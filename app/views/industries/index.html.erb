<%= render "topbar" %>
<div class='industry'>
  <ul>
    <% @industries.each do |industry| %>
      <li>
        <%= link_to image_tag(industry.image), new_industry_candidate_path(industry) %>
      </li>
    <% end %>
  </ul>
</div>
