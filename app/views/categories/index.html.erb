<%= image_tag('category-banner.png') %>
<div class='category'>
  <ul>
    <% @categories.each do |category| %>
      <li>
        <%= link_to image_tag(category.image), new_category_candidate_path(category) %>
      </li>
    <% end %>
  </ul>
</div>
